---
import { TopPage } from '@/components/views'
import Layout from '@/layouts/Layout.astro'

const isDev = process.env.NODE_ENV == 'development'
---

<Layout pageId={'index'}>
  <TopPage client:load />
</Layout>
