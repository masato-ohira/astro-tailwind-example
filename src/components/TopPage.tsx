import { tw } from '@/utils/tw'

export const TopPage = () => {
  return (
    <div
      className={tw(
        //
        'container',
        'py-8',
      )}
    >
      <div
        className={tw(
          //
          'btn',
          'btn-primary',
        )}
      >
        TopPage
      </div>
    </div>
  )
}
